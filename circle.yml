machine:
  ghc:
    version: 7.10.2

dependencies:
  cache_directories:
    - "frontend/node_modules"

  pre:
    - cd backend && cabal install && cabal build
    - cd frontend && npm install

test:
  post:
    - cd frontend && npm run test
