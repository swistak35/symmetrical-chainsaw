machine:
  ghc:
    version: 7.10.3

dependencies:
  cache_directories:
    - "frontend/node_modules"

  pre:
    - cd frontend && npm install
    - cd backend && cabal build

test:
  post:
    - cd frontend && npm run test
